// use crate::http::middlewares::test_middleware::TestMiddleware;

pub mod auth_middleware;

// pub fn middlewares() {
// let mut middlewares = Vec::new();

// middlewares.push(Box::new(TestMiddleware));
// }
